# multimodal_ai_biomedclip

This notebook implements a zero-shot image classification approach using CLIP, where images and text labels are mapped into a shared embedding space and compared using similarity scores. Without any task-specific training, the model predicts the most relevant medical labels for each image by ranking them according to their similarity. The results are then displayed by showing each image along with its top predicted labels for qualitative analysis.
